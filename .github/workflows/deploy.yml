name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t akash112005/hospital-app ./app

      - name: Push Docker image
        run: docker push akash112005/hospital-app

      # 🔥 FIXED SSH KEY METHOD
      - name: Create SSH key file
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > key.pem
          chmod 600 key.pem

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key_path: key.pem
          port: 22
          script: |
            docker stop $(docker ps -q) || true
            docker rm $(docker ps -aq) || true
            docker pull akash112005/hospital-app
            docker run -d -p 3000:3000 \
<<<<<<< HEAD
              -e MONGO_URI=mongodb+srv://akashUser:akash123@cluster0.lhizrdv.mongodb.net/?appName=Cluster0 \
              akash112005/hospital-app
=======
              -e MONGO_URI="${{ secrets.MONGO_URI }}" \
              akash112005/hospital-app
>>>>>>> f2e3b92 (SSH Resolved)
